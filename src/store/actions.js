import * as types from './mutation-types';

//  this lives in lobby.module but it's an example of actions which are module agnostic
// export const changeBingoTab = ({commit,}, groupID) => {
// 	commit(types.TAB_CHANGED, {
// 		Id: groupID,
// 	});
// };


